export const metadata = {
  title: 'Project Invitation',
  description: 'You have been invited to join a project on Tabloo.',
};

export default function InviteLayout({ children }) {
  return children;
}
